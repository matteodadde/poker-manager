# Imposta lo script di avvio per il comando 'flask run'
FLASK_APP=run.py

# Abilita la modalità Debug
# (Questo imposta automaticamente l'ambiente a "development",
# abilita il reloader e il debugger)
FLASK_DEBUG=1